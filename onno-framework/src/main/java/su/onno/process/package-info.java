/**
 * Typed business-process definition language and executable in-memory prototype.
 *
 * <p>The public API models route structure with Java enum step keys, typed node handles, and enum
 * task outcomes. The package does not yet provide durable process/work-item persistence, assignee
 * routing, timers, an inbox, or parallel/subprocess execution.</p>
 */
package su.onno.process;
