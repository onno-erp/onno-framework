# Baked at build time by onec-ui-starter/build.gradle.kts (processResources `expand`).
# The onec-framework version this UI starter was built from — read by OnecBuildInfo and
# compared against onec-cloud's announced latest release to drive the "update available" notice.
onec.version=${onecVersion}
