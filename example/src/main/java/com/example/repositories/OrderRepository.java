package com.example.repositories;

import com.example.domain.documents.Order;
import su.onno.repository.DocumentRepository;

/** Typed repository for {@link Order}. */
public interface OrderRepository extends DocumentRepository<Order> {
}
