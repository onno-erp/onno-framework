package com.example.repositories;

import com.example.domain.catalogs.BookCategory;
import su.onno.repository.CatalogRepository;

/** Typed repository for {@link BookCategory}. */
public interface BookCategoryRepository extends CatalogRepository<BookCategory> {
}
