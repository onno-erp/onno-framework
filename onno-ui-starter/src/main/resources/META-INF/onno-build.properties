# Baked at build time by onno-ui-starter/build.gradle.kts (processResources `expand`).
# The onno-framework version this UI starter was built from — read by OnnoBuildInfo and
# compared against onno-cloud's announced latest release to drive the "update available" notice.
onno.version=${onnoVersion}
